import React from "react";
import styled from "styled-components";

const Header = props => <Header4>{props.text}</Header4>;

const Header4 = styled.h4`
  margin: 5px;
`;

export default Header;
